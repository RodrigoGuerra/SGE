type Team {
  # Example field (placeholder)
  teamId: String!
  name: String!
  createdAt: String
  updatedAt: String
  discipline: Discipline!
  school: School!
}

input CreateTeamInput {
  # Example field (placeholder)
  teamId: String!
  name: String!
}

input UpdateTeamInput {
  teamId: String!
  name: String!
}
