type Team {
  # Example field (placeholder)
  teamId: String!
  name: String!
  disciplineId: String!
  schoolId: String!
  createdAt: String
  updatedAt: String
  
}

input CreateTeamInput {
  # Example field (placeholder)
  name: String!
  disciplineId: String!
  schoolId: String!
}

input UpdateTeamInput {
  teamId: String!
  name: String!
}
