type User {
  # Example field (placeholder)
  name: String
  email: String
  created_at: String
}

input CreateUserInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateUserInput {
  id: Int!
}
