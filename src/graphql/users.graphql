type User {
  # Example field (placeholder)
  userId: String!
  phone: String
  email: String!
  name: String!
  age: Int
  createdAt: String
  updatedAt: String
  roleId: Int!
  role: Role
}

input CreateUserInput {
  # Example field (placeholder)
  phone: String
  email: String!
  name: String!
  age: Int
  roleId: Int!
}

input UpdateUserInput {
  userId: String!
  phone: String
  email: String!
  name: String!
  age: Int
}
