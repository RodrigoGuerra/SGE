overwrite: true
schema: "_schema.graphql"
documents: null
generates:
  src/graphql/generated/graphql.ts:
    plugins:
      - typescript:
          skipTypename: true
          typesPrefix: Gql
          namingConvention:
            enumValues: keep
          declarationKind: interface